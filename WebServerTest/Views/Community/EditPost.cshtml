@model WebServerTest.Models.CreateEditPostViewModel
@{
    ViewData["Title"] = "Edit Post";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h3>Edit Post</h3>
                </div>
                <div class="card-body">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger">
                            @TempData["Error"]
                        </div>
                    }
                    
                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success">
                            @TempData["Success"]
                        </div>
                    }

                    <form asp-action="EditPost" method="post">
                        <input type="hidden" asp-for="PostId" />
                        <input type="hidden" asp-for="IsEditing" value="true" />
                        
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Title" class="form-label">Title</label>
                            <input asp-for="Title" class="form-control" placeholder="Enter a descriptive title" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Content" class="form-label">Content</label>
                            <textarea asp-for="Content" class="form-control" rows="8" placeholder="Write your post content here..."></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="CategoryId" class="form-label">Community (cannot be changed)</label>
                            <select asp-for="CategoryId" class="form-select" disabled>
                                @foreach (var category in Model.Categories)
                                {
                                    <option value="@category.Id" selected="@(category.Id == Model.CategoryId)">@category.Name</option>
                                }
                            </select>
                            <input type="hidden" asp-for="CategoryId" /> <!-- Hidden field to maintain the value -->
                            <div class="form-text text-muted">The community cannot be changed once a post is created.</div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="HashtagsString" class="form-label">Hashtags (separate with commas)</label>
                            <input asp-for="HashtagsString" class="form-control" placeholder="Enter hashtags separated by commas (e.g., travel, tips, food)" />
                            <div class="form-text text-muted">You can add up to 5 hashtags to help others find your post.</div>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Post" asp-route-id="@Model.PostId" class="btn btn-outline-secondary">Cancel</a>
                            <button type="submit" class="btn btn-primary">Update Post</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} 